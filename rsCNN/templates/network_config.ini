[paths]
dir_out = .
filepath_model = model.h5
filepath_history = history.json

[model]
checkpoint_periods = 5
verbosity = 1
assert_gpu = True

[training]
batch_size = 1
max_epochs = 100
n_noimprovement_repeats = 10
verification_fold = None
optimizer = adam

[tensorboard]
callbacks_use_tensorboard = True
dir_tensorboard_out = tensorboard
tensorboard_update_freq = epoch
tensorboard_histogram_freq = 0
tensorboard_write_graph = True
tensorboard_write_grads = False
tensorboard_write_images = True

[early_stopping]
callbacks_use_early_stopping = True
early_stopping_min_delta = 10**-4
early_stopping_patience = 50

[reduced_learning_rate]
callbacks_use_reduced_learning_rate = True
reduced_learning_rate_factor = 0.5
reduced_learning_rate_min_delta = 10**-4
reduced_learning_rate_patience = 10

[terminate_on_nan]
callbacks_use_terminate_on_nan = True
